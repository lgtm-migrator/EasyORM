package org.suntao.easyorm.map;

/**
 * 单独的sql语句记录
 * 
 * @author suntao
 *
 */
public class MapStatment {
	/**
	 * statment的ID 查询用
	 */
	private String id;
	/**
	 * statment的内容 将使用在statment中
	 */
	private String statmentStr;
	/**
	 * 查询变量
	 */
	private Object[] params;

	/**
	 * 创建一个新的MapStatment
	 * <p>
	 * 用于查询
	 * 
	 * @param id
	 *            本mapstatment 的ID
	 * @param statmentStr
	 *            用于查询的str
	 * @param params
	 *            parameters 变量
	 */
	public MapStatment(String id, String statmentStr, Object[] params) {
		super();
		this.id = id;
		this.statmentStr = statmentStr;
		this.params = params;
	}

	public MapStatment() {

	}

	public MapStatment(String id, String statmentStr) {
		super();
		this.id = id;
		this.statmentStr = statmentStr;
	}

	public String getId() {
		return id;
	}

	public void setId(String id) {
		this.id = id;
	}

	public String getStatmentStr() {
		return statmentStr;
	}

	public void setStatmentStr(String statmentStr) {
		this.statmentStr = statmentStr;
	}

	/**
	 * 返回一个完整的sql语句
	 * <p>
	 * 用于调试
	 * 
	 * @return sql语句
	 */
	public String getSqlStr() {
		String result = null;
		if (statmentStr != null) {
			// TO Do
		}
		return result;
	}
}
